# Navigation security decision

Use exact authentication hostnames and boundary-aware ChatGPT/OpenAI subdomains. Only HTTPS may remain inside the embedded ChatGPT view. Only HTTP and HTTPS may be delegated to the operating system.
